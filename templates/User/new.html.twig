{% extends 'base.html.twig' %}

{% block title %}Ajouter un user
{% endblock %}

{% block body %}
	<div class="container mt-4">
		<h1>Ajouter une nouvelle user</h1>

		{{ form_start(form) }}
		{{ form_widget(form) }}
		<button class="btn btn-success">Ajouter</button>
		{{ form_end(form) }}
	</div>
{% endblock %}
